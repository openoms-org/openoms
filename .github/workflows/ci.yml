name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GO_VERSION: "1.25.7"

jobs:
  auto-format:
    name: Auto-format
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install golangci-lint
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.9.0

      - name: Auto-fix Go code
        run: |
          for dir in apps/api-server packages/allegro-go-sdk packages/inpost-go-sdk packages/order-engine; do
            echo "Fixing $dir..."
            golangci-lint run --fix --timeout=5m "./$dir/..." || true
          done

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: apps/dashboard/package-lock.json

      - name: Auto-fix frontend code
        working-directory: apps/dashboard
        run: |
          npm ci
          npx eslint --fix src/ || true

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: auto-format code"
          commit_author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Lint api-server
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.9.0
          working-directory: apps/api-server
          args: --timeout=5m
          only-new-issues: true

      - name: Lint allegro-go-sdk
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.9.0
          working-directory: packages/allegro-go-sdk
          args: --timeout=5m
          only-new-issues: true

      - name: Lint inpost-go-sdk
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.9.0
          working-directory: packages/inpost-go-sdk
          args: --timeout=5m
          only-new-issues: true

      - name: Lint order-engine
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.9.0
          working-directory: packages/order-engine
          args: --timeout=5m
          only-new-issues: true

  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Scan api-server
        working-directory: apps/api-server
        run: govulncheck ./...

      - name: Scan allegro-go-sdk
        working-directory: packages/allegro-go-sdk
        run: govulncheck ./...

      - name: Scan inpost-go-sdk
        working-directory: packages/inpost-go-sdk
        run: govulncheck ./...

      - name: Scan order-engine
        working-directory: packages/order-engine
        run: govulncheck ./...

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: apps/dashboard/package-lock.json

      - name: Audit frontend dependencies
        working-directory: apps/dashboard
        run: npm ci && npm audit --audit-level=high

  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: openoms
          POSTGRES_PASSWORD: openoms_dev
          POSTGRES_DB: openoms_dev
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U openoms"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: postgres://openoms:openoms_dev@localhost:5432/openoms_dev?sslmode=disable
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: ci-test-jwt-secret-not-for-production
      ENCRYPTION_KEY: ci-test-encryption-key-32bytes!

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run migrations
        run: |
          PGPASSWORD=openoms_dev psql -h localhost -U openoms -d openoms_dev -f apps/api-server/migrations/000001_init_schema.up.sql

      - name: Test api-server
        working-directory: apps/api-server
        run: go test -race -cover ./...

      - name: Test erli-go-sdk
        working-directory: packages/erli-go-sdk
        run: go test ./...

      - name: Test allegro-go-sdk
        working-directory: packages/allegro-go-sdk
        run: go test ./...

      - name: Test inpost-go-sdk
        working-directory: packages/inpost-go-sdk
        run: go test ./...

      - name: Test order-engine
        working-directory: packages/order-engine
        run: go test ./...

      - name: Test dhl-go-sdk
        working-directory: packages/dhl-go-sdk
        run: go test ./...

      - name: Test ksef-go-sdk
        working-directory: packages/ksef-go-sdk
        run: go test ./...

      - name: Test amazon-sp-sdk
        working-directory: packages/amazon-sp-sdk
        run: go test ./...

      - name: Test dpd-go-sdk
        working-directory: packages/dpd-go-sdk
        run: go test ./...

      - name: Test gls-go-sdk
        working-directory: packages/gls-go-sdk
        run: go test ./...

      - name: Test btp-go-sdk
        working-directory: packages/btp-go-sdk
        run: go test ./...

  security-tests:
    name: Security Tests
    needs: [test]
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Run security tests
        working-directory: apps/api-server
        env:
          REDIS_URL: redis://localhost:6379
        run: go test -race -run 'TestSecurity_' ./internal/middleware/ ./internal/handler/

  vet:
    name: Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Vet api-server
        working-directory: apps/api-server
        run: go vet ./...

      - name: Vet allegro-go-sdk
        working-directory: packages/allegro-go-sdk
        run: go vet ./...

      - name: Vet inpost-go-sdk
        working-directory: packages/inpost-go-sdk
        run: go vet ./...

      - name: Vet order-engine
        working-directory: packages/order-engine
        run: go vet ./...

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: apps/dashboard/package-lock.json

      - name: Install dependencies
        working-directory: apps/dashboard
        run: npm ci

      - name: Lint
        working-directory: apps/dashboard
        run: npx eslint src/

      - name: Unit tests
        working-directory: apps/dashboard
        run: npx vitest run

      - name: Build
        working-directory: apps/dashboard
        run: npx next build

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [lint, test, security-tests, vet, frontend]
    steps:
      - uses: actions/checkout@v4

      - name: Build API server image
        run: docker build -f apps/api-server/Dockerfile -t openoms-api:ci .

      - name: Build Dashboard image
        run: docker build -f apps/dashboard/Dockerfile -t openoms-dashboard:ci ./apps/dashboard

  erli-integration:
    name: Erli SDK Integration Tests
    runs-on: ubuntu-latest
    if: ${{ secrets.ERLI_SANDBOX_TOKEN != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run Erli integration tests
        working-directory: packages/erli-go-sdk
        env:
          ERLI_SANDBOX_TOKEN: ${{ secrets.ERLI_SANDBOX_TOKEN }}
          ERLI_SANDBOX_OFFER_ID: ${{ secrets.ERLI_SANDBOX_OFFER_ID }}
          ERLI_SANDBOX_ORDER_ID: ${{ secrets.ERLI_SANDBOX_ORDER_ID }}
        run: go test -tags=integration -v -count=1 -timeout=5m ./...
